package Logic;

class KMeansTest {
   /* @Test
    void test() {

        KMeans clusterMachine = new KMeans();
        ArrayList<Point> arrayPoints = new ArrayList<Point>();

        for (int i = 0; i < 10000; i++) {
            double w = Utility.random(2,19);
            double x = Utility.random(10, 1500);
            double y = Utility.random(0,100);
            double z = Utility.random(0,4);
            Point p = new Point(w,x,y,z);
            arrayPoints.add(p);
        }

        clusterMachine.init(arrayPoints);
        ArrayList<Cluster> arrayCluster = clusterMachine.calculate();

        for (int i = 0; i < arrayCluster.size(); i++) {
            System.out.println(arrayCluster.get(i).points.size());
            System.out.println(arrayCluster.get(i).getAveragePoint().toString());
        }
    }*/
}